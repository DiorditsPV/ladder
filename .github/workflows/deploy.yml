name: Deploy

# Выкладка на сервер портфолио (ladder.paveldiordits.site) — ТОЛЬКО вручную: Actions → Deploy → Run workflow
# (или `gh workflow run Deploy --ref main`). Merge в main ничего не выкладывает: сервис живёт локально,
# сервер — необязательная публичная копия, обновляемая осознанно.
# Фронт собирается на раннере — node на сервере нет. Всё нужное уезжает одним tar.gz в stdin
# forced-command-скрипта /usr/local/bin/deploy-ladder.sh: ключ раннера на сервере умеет ровно
# эту команду (см. deploy/install.sh и DEPLOY.md). Host-key закреплён явно — подмену сервера
# раннер не примет.
on:
  workflow_dispatch: {}

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Собрать фронт (tsc + vite)
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Упаковать выкладку
        run: |
          tar czf ladder.tar.gz \
            --exclude='backend/.venv' --exclude='__pycache__' --exclude='*.db' --exclude='.pytest_cache' \
            backend content frontend/dist
          ls -la ladder.tar.gz

      - name: Разложить ключ и закреплённый host-key
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_LADDER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          printf '%s\n' "${{ vars.DEPLOY_HOST }} ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIF1sEThhqUNxeSWgjUtOOuulxk6EMVNAizFdjoCAPlXY" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Выложить на сервер
        run: |
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=yes \
              -o UserKnownHostsFile=~/.ssh/known_hosts \
              -o ConnectTimeout=20 \
              deploy@${{ vars.DEPLOY_HOST }} < ladder.tar.gz

      - name: Проверить боевой адрес
        run: |
          sleep 2
          base=https://ladder.paveldiordits.site
          code=$(curl -s -m 30 -o /dev/null -w '%{http_code}' "$base/api/health")
          [ "$code" = 200 ] || { echo "✗ /api/health → $code"; exit 1; }
          code=$(curl -s -m 30 -o /dev/null -w '%{http_code}' "$base/api/pools")
          [ "$code" = 200 ] || { echo "✗ /api/pools без cookie → $code (ожидалось 200: список демо-направлений)"; exit 1; }
          code=$(curl -s -m 30 -o /dev/null -w '%{http_code}' "$base/api/graph?pool=data-engineer")
          [ "$code" = 200 ] || { echo "✗ демо-граф без cookie → $code (ожидалось 200)"; exit 1; }
          code=$(curl -s -m 30 -o /dev/null -w '%{http_code}' "$base/api/graph?pool=data-engineer-x5")
          [ "$code" = 401 ] || { echo "✗ не-демо граф без cookie → $code (ожидалось 401)"; exit 1; }
          echo "✓ health 200, демо 200, остальное за логином"
