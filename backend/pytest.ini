[pytest]
# Тесты импортируют пакет `app` (backend/app), значит backend/ должен быть в sys.path.
# `python -m pytest` кладёт туда cwd сам, а консольный `pytest` — нет: из-за этого
# документированный в README/CLAUDE.md `pytest -q` падал с ModuleNotFoundError: app.
pythonpath = .
