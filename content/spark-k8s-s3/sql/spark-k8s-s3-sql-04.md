---
block: sql
difficulty: basics
id: spark-k8s-s3-sql-04
kind: question
subblock: joins
tags:
- distributed
- sql
title: Стратегии джойна в Spark
topic: join-strategies
weight: 1
---

## Вопрос
Какие стратегии джойна есть в Spark и чем broadcast hash join отличается от sort-merge join?

## Ответ
Стратегия джойна — способ свести вместе строки с одинаковым ключом, когда стороны лежат на разных экзекьюторах. Главных две.

**Broadcast hash join** (`BroadcastHashJoin`). Маленькая сторона собирается на драйвере, из неё строится хеш-таблица, и копия уходит каждому экзекьютору. Большая сторона не двигается: каждый таск читает свой кусок и ищет пары в хеш-таблице. Shuffle большой стороны нет — это самый быстрый вариант, пока маленькая сторона помещается в память.

**Sort-merge join** (`SortMergeJoin`). Обе стороны перемешиваются shuffle по ключу, каждая партиция сортируется, затем два отсортированных потока сливаются. Работает на любых объёмах и при нехватке памяти уходит в spill, но платит двумя shuffle и сортировкой.

Есть ещё **shuffled hash join** — shuffle как у sort-merge, но вместо сортировки в партиции строится хеш-таблица из меньшей стороны, — и **nested loop** для условий без равенства (`a.ts BETWEEN b.from AND b.to` без общего ключа): каждая строка сравнивается с каждой, на больших таблицах это катастрофа.

Без подсказок Spark берёт broadcast, если оценка размера стороны ниже порога, иначе sort-merge. Выбор видно в `df.explain()` по узлу над сканами; AQE после shuffle может заменить sort-merge на broadcast, итог — во вкладке SQL в Spark UI.
